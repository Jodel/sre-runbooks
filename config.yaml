baseURL: "https://jodel.github.io/sre-runbooks/"
languageCode: "en-us"
title: "Jodel SRE Runbooks"

theme: book

params:
  BookRepo: "https://github.com/Jodel/sre-runbooks"
  BookEditPath: "edit/main"
  BookSearch: true
  BookSection: "runbooks"
  #BookMenuBundle: "/menu"

markup:
  goldmark:
    renderer:
      # Allows rendering HTML tags. This would only be truly "unsafe" if we were accepting untrusted user input and rendering it, which we aren't.
      unsafe: true
